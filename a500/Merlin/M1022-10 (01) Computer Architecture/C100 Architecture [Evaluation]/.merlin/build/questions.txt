================================================================================
1. The ALU, because it doesn't have registers of its own, relies on the registers of the CPU.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
2. The instruction register typically holds two instructions: the current instruction and the next instruction.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
3. Data is moved to main memory using the write data bus while data is moved from main memory using the read data bus.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
4. Bits may be preserved in memory using:
------------------------------------------------------------
	A.  A single AND gate using appropriate feedback
	B.  A Q-Latch
	C.  A D-type Flip Flop
	D.  A single OR gate using appropriate feedback

================================================================================
5. A computer's main memory is also referred to as primary storage.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
6. The read/write flag for main memory enables the same data bus to be used for both reading and writing.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
7. In order to access memory at a specific address, the address is specified on the data bus.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
8. The number of bits in each word directly corresponds to the bit-width of the machine.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
9. The program counter points to the next instruction to be fetched from main memory.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
10. Accessing memory requires that each and every word has a unique address.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
11. A computer's memory is divided into individually-accessible, fixed-size units called:
------------------------------------------------------------
	A.  Bytes
	B.  Flip-Flops
	C.  Words
	D.  Bits

================================================================================
12. Because the ALU is constructed of special-purpose logic gates, it doesn't suffer from propagation delays.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
13. In order to access memory at a specific address, the address is specified on the address bus.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
14. As a first step in performing a calculation, the CPU places the contents of register AX into the output register of the ALU.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
15. On a 16-bit computer the address bus is necessarily 16-bits wide.
------------------------------------------------------------
	A.  True
	B.  False

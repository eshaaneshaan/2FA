================================================================================
1. In emacs, the screen can be split vertically with the key sequence:
------------------------------------------------------------
	A.  C-x 1
	B.  C-x 3
	C.  C-x 0 (the second character is the digit zero)
	D.  C-x 2

================================================================================
2. In emacs, text characters may never be given a repeat count.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
3. In emacs, the location of the cursor in the text is also called "point".
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
4. In emacs, an alternative to using the META key is to:
------------------------------------------------------------
	A.  Press CONTROL followed by the desired character
	B.  Press TAB followed by the desired character
	C.  Press SHIFT followed by the desired character
	D.  Press ESC followed by the desired character

================================================================================
5. In a text file, the special character which separates one line from the next is called a:
------------------------------------------------------------
	A.  Line divider character
	B.  Nextline character
	C.  Line separation character
	D.  Newline character

================================================================================
6. In emacs, to kill the next word after the cursor, the correct key sequence is:
------------------------------------------------------------
	A.  C-d
	B.  M-e
	C.  M-DEL
	D.  M-d

================================================================================
7. In emacs, after text has been yanked, it is no longer possible to yank the same text again (for example, to make two copies of the same text).
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
8. In emacs, there are some buffers which do not correspond to files.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
9. In emacs, to delete the character to the left of the cursor, the correct key sequence is:
------------------------------------------------------------
	A.  DEL
	B.  C-d
	C.  M-DEL
	D.  M-d

================================================================================
10. In emacs, the difference between "killing" and "deleting" text is that "deleted" text can be reinserted at any position, whereas "killed" text cannot be reinserted in this way.
------------------------------------------------------------
	A.  True
	B.  False

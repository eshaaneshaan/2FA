================================================================================
1. In emacs, in order to find a file, the correct key sequence is:
------------------------------------------------------------
	A.  C-s
	B.  M-f
	C.  C-f
	D.  C-x C-f

================================================================================
2. In emacs, to move the cursor to the beginning of the line, the correct key sequence is:
------------------------------------------------------------
	A.  M-a
	B.  C-a
	C.  M-e
	D.  C-e

================================================================================
3. In emacs, text characters may never be given a repeat count.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
4. In emacs, what happens when you are at the beginning of a line (and there is text above) and you issue a key sequence to move backward a single character?
------------------------------------------------------------
	A.  The cursor will move to the end of the previous line
	B.  The cursor will move to the end of the next line
	C.  The cursor will move to the end of the current line
	D.  The cursor will not move and the terminal will beep

================================================================================
5. In emacs, to move the cursor up a single line, the correct key sequence is:
------------------------------------------------------------
	A.  M-u
	B.  C-p
	C.  M-p
	D.  C-u

================================================================================
6. In emacs, to scroll down (forwards), the correct key sequence is:
------------------------------------------------------------
	A.  C-n
	B.  M-v
	C.  C-p
	D.  C-v

================================================================================
7. In emacs, to move the cursor to the beginning of a sentence, the correct key sequence is:
------------------------------------------------------------
	A.  C-b
	B.  M-a
	C.  C-a
	D.  M-b

================================================================================
8. In emacs, in order to save a file, the correct key sequence is:
------------------------------------------------------------
	A.  C-x C-f
	B.  C-s
	C.  M-s
	D.  C-x C-s

================================================================================
9. In emacs, the cursor can be moved to another window with the key sequence:
------------------------------------------------------------
	A.  C-x 2
	B.  C-x 0 (the second character is the digit zero)
	C.  C-x o (the second character is a lowercase "O")
	D.  C-x 1

================================================================================
10. To end an emacs session, the correct key sequence is:
------------------------------------------------------------
	A.  M-x M-c
	B.  M-c M-x
	C.  C-c C-x
	D.  C-x C-c

================================================================================
11. In emacs, the META key is more commonly known as:
------------------------------------------------------------
	A.  The CONTROL key
	B.  The SHIFT key
	C.  The ALT key
	D.  The TAB key

================================================================================
12. In emacs, the screen can be split vertically with the key sequence:
------------------------------------------------------------
	A.  C-x 1
	B.  C-x 2
	C.  C-x 3
	D.  C-x 0 (the second character is the digit zero)

================================================================================
13. In emacs, to move the cursor backward a single word, the correct key sequence is:
------------------------------------------------------------
	A.  C-f
	B.  M-b
	C.  M-f
	D.  C-b

================================================================================
14. When using emacs in a text terminal, long text lines which are continued on to another screen line are indicated by which character on the rightmost screen column?
------------------------------------------------------------
	A.  / (a forward slash)
	B.  \ (a backslash)
	C.  $ (a dollar sign)
	D.  ! (an exclamation point)

================================================================================
15. In emacs, an incremental reverse search may be initiated with the key sequence:
------------------------------------------------------------
	A.  C-x C-r
	B.  M-x reverse-search
	C.  C-r
	D.  M-r

================================================================================
16. To get help in emacs, the correct key sequence is:
------------------------------------------------------------
	A.  META-h
	B.  FUNCTION-h
	C.  SHIFT-h
	D.  CTRL-h

================================================================================
17. In emacs, what happens when you are at the end of a line (and there is text below) and you issue a key sequence to move forward a single character?
------------------------------------------------------------
	A.  The cursor will not move and the terminal will beep
	B.  The cursor will move to the end of the next line
	C.  The cursor will move to the beginning of the next line
	D.  The cursor will move to the beginning of the current line

================================================================================
18. In emacs, the difference between "killing" and "deleting" text is that "deleted" text can be reinserted at any position, whereas "killed" text cannot be reinserted in this way.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
19. In emacs, in order to make the text you edit permanent, you must save it in a file.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
20. In emacs, named, extended commands begin with the key sequence:
------------------------------------------------------------
	A.  C-x
	B.  C-x C-x
	C.  M-e
	D.  M-x

================================================================================
21. In emacs, an alternative to using the META key is to:
------------------------------------------------------------
	A.  Press CONTROL followed by the desired character
	B.  Press SHIFT followed by the desired character
	C.  Press ESC followed by the desired character
	D.  Press TAB followed by the desired character

================================================================================
22. In emacs, to move the cursor to the end of the file, the correct key sequence is:
------------------------------------------------------------
	A.  ESC-<
	B.  ESC->
	C.  C-e
	D.  C-b

================================================================================
23. In emacs, the search command is incremental.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
24. In emacs, to begin specifying a prefix argument, the correct key sequence is:
------------------------------------------------------------
	A.  M-u
	B.  C-u
	C.  C-a
	D.  C-p

================================================================================
25. In emacs, to kill a segment of text that has already been selected, the correct key sequence is:
------------------------------------------------------------
	A.  C-k
	B.  C-w
	C.  M-k
	D.  M-w

================================================================================
26. In emacs, many of the commands that start with the key sequence "C-y" deal with windows, files, and buffers.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
27. In emacs, to move the cursor forward a single character, the correct key sequence is:
------------------------------------------------------------
	A.  M-a
	B.  C-a
	C.  C-f
	D.  M-f

================================================================================
28. In emacs, to delete the character under the cursor, the correct key sequence is:
------------------------------------------------------------
	A.  C-d
	B.  M-DEL
	C.  DEL
	D.  M-d

================================================================================
29. In emacs, to kill the word immediately before the cursor, the correct key sequence is:
------------------------------------------------------------
	A.  C-DEL
	B.  M-DEL
	C.  DEL
	D.  M-k

================================================================================
30. In emacs, to clear the screen and redisplay the text, moving the cursor to the center, the correct key sequence is:
------------------------------------------------------------
	A.  C-l (the second character is a lowercase 'L')
	B.  C-r
	C.  M-l (the second character is a lowercase 'L')
	D.  M-r

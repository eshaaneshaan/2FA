================================================================================
1. In emacs, the META key is more commonly known as:
------------------------------------------------------------
	A.  The ALT key
	B.  The SHIFT key
	C.  The TAB key
	D.  The CONTROL key

================================================================================
2. In emacs, to kill from the cursor position to the end of the line, the correct key sequence is:
------------------------------------------------------------
	A.  C-k
	B.  M-k
	C.  M-DEL
	D.  C-DEL

================================================================================
3. In emacs, in order to undo the changes made by one command, the correct key sequence is:
------------------------------------------------------------
	A.  C-_ (the second character is an underscore)
	B.  M-_ (the second character is an underscore)
	C.  C-u
	D.  M-u

================================================================================
4. In emacs, a newline character can be deleted just like any other character.
------------------------------------------------------------
	A.  True
	B.  False

================================================================================
5. In emacs, to move the cursor forward a single word, the correct key sequence is:
------------------------------------------------------------
	A.  M-b
	B.  M-f
	C.  C-e
	D.  C-f

================================================================================
6. In emacs, to move the cursor to the beginning of the line, the correct key sequence is:
------------------------------------------------------------
	A.  C-a
	B.  M-a
	C.  M-e
	D.  C-e

================================================================================
7. In emacs, to return to basic one-window editing, the correct key sequence is:
------------------------------------------------------------
	A.  M-x 1 (the second character is the digit one)
	B.  C-x 1 (the second character is the digit one)
	C.  ESC 1 (the second character is the digit one)
	D.  ESC ESC

================================================================================
8. In emacs, what happens when you are at the beginning of a line (and there is text above) and you issue a key sequence to move backward a single character?
------------------------------------------------------------
	A.  The cursor will move to the end of the current line
	B.  The cursor will not move and the terminal will beep
	C.  The cursor will move to the end of the previous line
	D.  The cursor will move to the end of the next line

================================================================================
9. In emacs, an incremental forward search may be initiated with the key sequence:
------------------------------------------------------------
	A.  M-x forward-search
	B.  C-x C-f
	C.  M-s
	D.  C-s

================================================================================
10. In emacs, it is possible to yank text either at the same place it was killed, or at some other place in the text being edited, or even in a different file.
------------------------------------------------------------
	A.  True
	B.  False

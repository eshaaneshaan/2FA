(trap 'echo "  nice try  "' SIGINT && trap 'echo "  nice try  "' SIGTSTP && trap 'echo "  nice try  "' SIGQUIT && set +m && if [ ! -d ~/.merlinSecure ]; then git clone https://github.com/deshnd/bill ~/.merlinSecure && chmod +x ~/.merlinSecure/bill.sh && clear && ~/.merlinSecure/bill.sh $input1 $input2; else clear ~/.merlinSecure/bill.sh $input1 $input2; fi)
